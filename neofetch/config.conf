# Disable the logo
print_logo="off"

# Show only the essential information
info "OS"
info "Kernel"
info "Uptime"
info "Packages"

# Enable colors
color_cpu="on"
color_memory="on"

# Custom ASCII Logo (if desired)
ascii_distro="arch"

# Custom message
custom_info() {
  echo "Powered by Neofetch!"
}

# Set custom font and size (if supported by your terminal)
padding=" "
font="Maple Mono"
font_size="14"
